import MainForm from "@/components/MainForm";

export default function App() {
  return (
    <main className="flex flex-col gap-3 min-h-screen w-full max-w-3xl m-auto p-8">
      <MainForm />
    </main>
  );
}
